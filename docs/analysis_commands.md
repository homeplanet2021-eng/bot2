# Analysis command log

Commands run to review the current scaffold for the user request:

- `rg -n "trial|promo|referr|ticket|adguard|admin|notification|payment|intent|webhook|outbox" -S /workspace/bot2/app`
